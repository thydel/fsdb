# generated by bajl:as-lib, avoid edit
shopt -s expand_aliases
alias fsdb='ns:fsdb'
alias ingest-stat='fsdb:ingest-stat'
alias files-stat='fsdb:files-stat'
alias path-to-name='fsdb:path-to-name'
alias list-deleted='fsdb:list-deleted'
alias add-snapshot='fsdb:add-snapshot'
alias view-snapshot-1='fsdb:view-snapshot-1'
alias view-snapshot-2='fsdb:view-snapshot-2'
alias view-snapshot='fsdb:view-snapshot'
alias prune-dedup='fsdb:prune-dedup'
alias mark-deleted='fsdb:mark-deleted'
alias use='fsdb:use'
alias purge-before='fsdb:purge-before'
fsdb:add-snapshot () 
{ 
    fsdb:ingest-stat "$@";
    fsdb:prune-dedup "$@";
    fsdb:mark-deleted "$@"
}
fsdb:files-stat () 
{ 
    local perl='BEGIN { $t = time; $j = JSON::PP->new->utf8 }
chomp;
@s = lstat($_);
next unless @s;
print $j->encode([$t, $s[9], $s[7], $_]), "\n"';
    ( cd ${1:?} && find -type f -print0 | perl -MJSON::PP -0 -ne "$perl" )
}
fsdb:ingest-stat () 
{ 
    local sql='CREATE TABLE IF NOT EXISTS fsdb (
  date TIMESTAMP,
  mdate TIMESTAMP,
  size BIGINT,
  suf VARCHAR,
  path VARCHAR[]
);

INSERT INTO fsdb SELECT
  to_timestamp((js.json->0)::BIGINT),
  to_timestamp((js.json->1)::BIGINT),
  (js.json->2)::BIGINT,
  regexp_extract(js.json->>3, '\''\.([^./]+)$'\'', 1),
  list_filter(string_split(js.json->>3, '\''/'\''), x -> x != '\''.'\'')
FROM read_json_auto('\''/dev/stdin'\'') js';
    : ${2:?};
    fsdb:files-stat $1 | duckdb $2/$(path-to-name $1).db -c "$sql"
}
fsdb:list-deleted () 
{ 
    local jq='SELECT * from fsdb WHERE size = NULL';
    duckdb $2/$(path-to-name $1).db -c "$sql" "${@:3}"
}
fsdb:mark-deleted () 
{ 
    local sql='INSERT INTO fsdb
WITH meta AS (
  SELECT
    MAX(date) as curr_t,
    MAX(date) FILTER (WHERE date < (SELECT MAX(date) FROM fsdb)) as prev_t
  FROM fsdb
),
deleted_paths AS (
  -- Files present in Previous but missing in Current
  SELECT path FROM fsdb, meta WHERE date = meta.prev_t
  EXCEPT
  SELECT path FROM fsdb, meta WHERE date = meta.curr_t
)
SELECT
  meta.curr_t AS date,
  NULL AS mdate,
  NULL AS size,  -- The Deletion Marker
  NULL AS suf,
  path
FROM deleted_paths, meta';
    duckdb $2/$(path-to-name $1).db -c "$sql"
}
fsdb:path-to-name () 
{ 
    local jq='. / "/" | map(select(length > 0)) | join("-")';
    bajl:self -Rr <<< ${1:?}
}
fsdb:prune-dedup () 
{ 
    local sql='DELETE FROM fsdb
WHERE rowid IN (
    SELECT rowid FROM (
        SELECT
            rowid,
            date,
            size,
            LAG(size) OVER (PARTITION BY path ORDER BY date) AS prev_size
        FROM fsdb
    )
    WHERE size = prev_size
      -- CRITICAL: Protect the most recent snapshot regardless of duplication
      AND date < (SELECT MAX(date) FROM fsdb)
)';
    : ${2:?};
    duckdb $2/$(path-to-name $1).db -c "$sql"
}
fsdb:purge-before () 
{ 
    local sql='DELETE FROM fsdb WHERE date < '\''\($limit)'\''::TIMESTAMP';
    : ${2:?};
    duckdb $2/$(path-to-name $1).db -c "$sql"
}
fsdb:src () 
{ 
    jq <<< '[{"ns":"fsdb","id":"use","sh":"source <(eval \"$@\")"},{"ns":"fsdb","id":"files-stat","perl":"BEGIN { $t = time; $j = JSON::PP->new->utf8 }\nchomp;\n@s = lstat($_);\nnext unless @s;\nprint $j->encode([$t, $s[9], $s[7], $_]), \"\\n\"","sh":"(cd ${1:?} && find -type f -print0 | perl -MJSON::PP -0 -ne \"$perl\")"},{"ns":"fsdb","id":"path-to-name","jq":". / \"/\" | map(select(length > 0)) | join(\"-\")","sh":"<<< ${1:?} self -Rr"},{"ns":"fsdb","id":"ingest-stat","sql":"CREATE TABLE IF NOT EXISTS fsdb (\n  date TIMESTAMP,\n  mdate TIMESTAMP,\n  size BIGINT,\n  suf VARCHAR,\n  path VARCHAR[]\n);\n\nINSERT INTO fsdb SELECT\n  to_timestamp((js.json->0)::BIGINT),\n  to_timestamp((js.json->1)::BIGINT),\n  (js.json->2)::BIGINT,\n  regexp_extract(js.json->>3, '\''\\.([^./]+)$'\'', 1),\n  list_filter(string_split(js.json->>3, '\''/'\''), x -> x != '\''.'\'')\nFROM read_json_auto('\''/dev/stdin'\'') js","sh":": ${2:?}; files-stat $1 | duckdb $2/$(path-to-name $1).db -c \"$sql\""},{"ns":"fsdb","id":"purge-before","sql":"DELETE FROM fsdb WHERE date < '\''\\($limit)'\''::TIMESTAMP","sh":": ${2:?}; duckdb $2/$(path-to-name $1).db -c \"$sql\""},{"ns":"fsdb","id":"prune-dedup","sql":"DELETE FROM fsdb\nWHERE rowid IN (\n    SELECT rowid FROM (\n        SELECT\n            rowid,\n            date,\n            size,\n            LAG(size) OVER (PARTITION BY path ORDER BY date) AS prev_size\n        FROM fsdb\n    )\n    WHERE size = prev_size\n      -- CRITICAL: Protect the most recent snapshot regardless of duplication\n      AND date < (SELECT MAX(date) FROM fsdb)\n)","sh":": ${2:?}; duckdb $2/$(path-to-name $1).db -c \"$sql\""},{"ns":"fsdb","id":"mark-deleted","sql":"INSERT INTO fsdb\nWITH meta AS (\n  SELECT\n    MAX(date) as curr_t,\n    MAX(date) FILTER (WHERE date < (SELECT MAX(date) FROM fsdb)) as prev_t\n  FROM fsdb\n),\ndeleted_paths AS (\n  -- Files present in Previous but missing in Current\n  SELECT path FROM fsdb, meta WHERE date = meta.prev_t\n  EXCEPT\n  SELECT path FROM fsdb, meta WHERE date = meta.curr_t\n)\nSELECT\n  meta.curr_t AS date,\n  NULL AS mdate,\n  NULL AS size,  -- The Deletion Marker\n  NULL AS suf,\n  path\nFROM deleted_paths, meta","sh":"duckdb $2/$(path-to-name $1).db -c \"$sql\""},{"ns":"fsdb","id":"list-deleted","jq":"SELECT * from fsdb WHERE size = NULL","sh":"duckdb $2/$(path-to-name $1).db -c \"$sql\" \"${@:3}\""},{"ns":"fsdb","id":"add-snapshot","sh":"ingest-stat \"$@\"\nprune-dedup \"$@\"\nmark-deleted \"$@\""},{"ns":"fsdb","id":"view-snapshot","sql":"WITH boundary AS (SELECT MAX(date) as max_d FROM fsdb)\nSELECT\n  LEAST('\''\\($target)'\''::TIMESTAMP, boundary.max_d) AS date,\n  arg_max(mdate, date) AS mdate,\n  arg_max(size, date) AS size,\n  path\nFROM fsdb, boundary\nWHERE fsdb.date <= '\''\\($target)'\''::TIMESTAMP\nGROUP BY path, boundary.max_d\nHAVING size IS NOT NULL","sh":"local target=\"${1:-now}\"\nlocal db=$(DB-files-stat \"$@\")\njq -nr \"\\\"$sql\\\"\" \"\" --arg target \"$target\" | duckdb -nullvalue '\'' '\'' \"$db\""},{"ns":"fsdb","id":"view-snapshot-2","sql":"WITH boundary AS (SELECT MAX(date) as max_d FROM fsdb)\nSELECT\n  -- Cast input string to timestamp; clamp to DB end if future\n  LEAST('\''\\($target)'\''::TIMESTAMP, boundary.max_d) AS date,\n  arg_max(mdate, date) AS mdate,\n  arg_max(size, date) AS size,\n  path\nFROM fsdb, boundary\nWHERE fsdb.date <= '\''\\($target)'\''::TIMESTAMP\nGROUP BY path, boundary.max_d","sh":"local target=\"${1:?}\"; shift\n#local db=$(DB-files-stat \"$@\")\n#jq -nr \"\\\"$sql\\\"\" \"\" --arg target \"$target\" | duckdb -nullvalue '\'' '\'' \"$db\"\njq -nr \"\\\"$sql\\\"\" \"\" --arg target \"$target\" | duckdb tmp/2.db"},{"ns":"fsdb","id":"view-snapshot-1","sql":"SELECT\n  '\''\\($target)'\''::TIMESTAMP AS date,\n  arg_max(mdate, date) AS mdate,\n  arg_max(size, date) AS size,\n  path\nFROM fsdb\nWHERE date <= '\''\\($target)'\''::TIMESTAMP\nGROUP BY path","sh":"local target=\"${1:?}\"; shift\n#local db=$(DB-files-stat \"$@\")\n#jq -nr \"\\\"$sql\\\"\" \"\" --arg target \"$target\" | duckdb -nullvalue '\'' '\'' \"$db\"\njq -nr \"\\\"$sql\\\"\" \"\" --arg target \"$target\" | duckdb -nullvalue '\'' '\'' tmp/2.db"}]'
}
fsdb:use () 
{ 
    source <(eval "$@")
}
fsdb:view-snapshot () 
{ 
    local sql='WITH boundary AS (SELECT MAX(date) as max_d FROM fsdb)
SELECT
  LEAST('\''\($target)'\''::TIMESTAMP, boundary.max_d) AS date,
  arg_max(mdate, date) AS mdate,
  arg_max(size, date) AS size,
  path
FROM fsdb, boundary
WHERE fsdb.date <= '\''\($target)'\''::TIMESTAMP
GROUP BY path, boundary.max_d
HAVING size IS NOT NULL';
    local target="${1:-now}";
    local db=$(DB-files-stat "$@");
    jq -nr "\"$sql\"" "" --arg target "$target" | duckdb -nullvalue ' ' "$db"
}
fsdb:view-snapshot-1 () 
{ 
    local sql='SELECT
  '\''\($target)'\''::TIMESTAMP AS date,
  arg_max(mdate, date) AS mdate,
  arg_max(size, date) AS size,
  path
FROM fsdb
WHERE date <= '\''\($target)'\''::TIMESTAMP
GROUP BY path';
    local target="${1:?}";
    shift;
    jq -nr "\"$sql\"" "" --arg target "$target" | duckdb -nullvalue ' ' tmp/2.db
}
fsdb:view-snapshot-2 () 
{ 
    local sql='WITH boundary AS (SELECT MAX(date) as max_d FROM fsdb)
SELECT
  -- Cast input string to timestamp; clamp to DB end if future
  LEAST('\''\($target)'\''::TIMESTAMP, boundary.max_d) AS date,
  arg_max(mdate, date) AS mdate,
  arg_max(size, date) AS size,
  path
FROM fsdb, boundary
WHERE fsdb.date <= '\''\($target)'\''::TIMESTAMP
GROUP BY path, boundary.max_d';
    local target="${1:?}";
    shift;
    jq -nr "\"$sql\"" "" --arg target "$target" | duckdb tmp/2.db
}
ns:fsdb () 
{ 
    fsdb:${1:?} "${@:2}"
}
bajl:self () 
{ 
    jq "$jq" "$@"
}
export -f fsdb:add-snapshot fsdb:files-stat fsdb:ingest-stat fsdb:list-deleted fsdb:mark-deleted fsdb:path-to-name fsdb:prune-dedup fsdb:purge-before fsdb:src fsdb:use fsdb:view-snapshot fsdb:view-snapshot-1 fsdb:view-snapshot-2 ns:fsdb
eval "$@"
